<div class="flex flex-wrap justify-center gap-2 ">
{% for activity in activities %}
<span class="py-2 px-6 bg-sky-100 text-blue-800 rounded-3xl shrink-0 text-sm">{{activity}}</span>
{% endfor %}
</div>
