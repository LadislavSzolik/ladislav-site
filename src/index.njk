---
layout: base
title: Ladislav Szolik
description: Ladislav Szolik, Senior UX Expert at Ärztekasse. Former mobile and web UX designer at Swisscom and Credit Suisse, with software engineering experience at Accenture. Slovak-born, Zürich-based.
---

<style type="text/css">
  .hero {
    background: #ffe6ea;
    background: linear-gradient(34deg, rgba(255, 230, 234, 1) 0%, rgba(203, 234, 242, 1) 100%);
  }
  .hero-links {
    display: flex;
    gap: 0.5rem;
    padding-block-start: 1rem;
  }

  .hero-text-links {
    padding-inline: 1rem;
  }

  .hero-text-links-img {
    display: flex;
    padding: 4rem 1rem;
    flex-direction: column-reverse;
    align-items: center;
    max-width: var(--max-width);
    margin-inline: auto;
  }

  .hero-text {
    font-size: 1.2rem;
    font-weight: 400;
    line-height: 1.6;
    color: var(--black);
    margin-bottom: 1rem;
  }

  .hero-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    & a {
      font-size: 1.1rem;
      background-color: var(--white-80);
      padding: 0.5rem 1.25rem;
      border-radius: 0.5rem;
      text-decoration: none;
    }
    & a:hover {
      background-color: var(--blue-hover-2);
    }
  }
  .under-wave {
    text-decoration: underline;
    text-decoration-style: wavy;
    text-decoration-color: rgb(239, 177, 188);
    text-decoration-thickness: 3px;
    text-underline-offset: 8px;
  }

  @media (min-width: 640px) {
    .hero-text {
      font-size: 1.8rem;
    }
    .hero-text-links-img {
      padding: 6rem 0;
      flex-direction: row;
      gap: 5rem;
      align-items: flex-start;            
    }
  }
</style>

<header class="hero">
  <div class="hero-text-links-img">
    <section class="hero-text-links">
      <h1 class="hero-text">
        Ladislav Szolik, Senior UX Expert at Ärztekasse. Former mobile and web
        <span class="under-wave">UX designer</span> at Swisscom and Credit Suisse, with
        <span class="under-wave">software engineering</span> experience at Accenture. Slovak-born, Zürich-based.
      </h1>
      <div class="hero-links">
        <a class="link" href="/services">Services</a>
        <a class="link" href="/portfolio">Portfolio</a>
        <a class="link" href="https://www.linkedin.com/in/ladislav-szolik-30278593/" target="_blank" rel="noreferrer"
          >LinkedIn</a
        >
      </div>
    </section>
    <img src="/images/profile_photo.webp" alt="Profile picture" width="344" height="344" />
  </div>
</header>

<style>
  .services-container {
    max-width: var(--max-width);
    margin: 4rem auto;

    & h1 {
      margin-bottom: 1.5rem;
      text-align: center;
    }
    & ul {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 2rem;      
    }

    & li {
      list-style: none;
    }
  }

  .services-container ul li:nth-child(1) img {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
  }

  .services-container ul li:nth-child(2) img {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
  }

  .services-container ul li:nth-child(3) img {
    position: absolute;
    bottom: 80px;
    right: 0;
  }

  .service-card {
    position: relative;
    width: 280px;
    height: 470px;
    background-color: rgb(206, 230, 236);
    filter: drop-shadow(4px 8px 4px rgba(97, 97, 97, 0.1)) contrast(1.1);
    transition: transform 0.3s cubic-bezier(0, 0, 0.5, 1), filter 0.3s cubic-bezier(0, 0, 0.5, 1);
    cursor: pointer;
    padding: 2rem;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 2rem;
    color: var(--black);
    font-size: 1rem;
    text-decoration: none;
    & h3 {
      margin-bottom: 0.5rem;
    }
    &:hover {
      transform: scale(1.02);
      filter: drop-shadow(8px 10px 4px rgba(97, 97, 97, 0.1)) contrast(1.15);
    }
  }
</style>
<section class="services">
  <div class="services-container">
    <h1>{{ services.title }}</h1>
    <ul>
      {% for service in services.serviceList %}
      <li>
        <a href="{{service.link}}" class="service-card">
          <div class="service-content">
            <h3>{{ service.title }}</h3>
            <p>{{ service.description }} <u>Learn more</u></p>
          </div>
          <img src="{{ service.image | url }}" width="180" alt="{{ service.title }}" />
        </a>
      </li>
      {% endfor %}
    </ul>
  </div>
</section>

{% include "partials/portfolio-list.njk" %}

{% include "contact-form.njk" %}
